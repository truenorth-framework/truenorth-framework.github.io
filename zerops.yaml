zerops:
  - setup: web
    build:
      base: nodejs@22
      buildCommands:
        - npm install
        - npm run build
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      start: npm start
